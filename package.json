{
  "name": "los-mecanismos-invisibles",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/build-essays.js",
    "dev": "vite",
    "prebuild": "node scripts/build-essays.js",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.43.0",
    "mammoth": "^1.12.0",
    "vite": "^8.1.1",
    "wrangler": "^4.107.0"
  }
}
